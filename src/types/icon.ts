// src/types/icon.ts
import type { CSSProperties } from 'react';

export interface IconProps {
  className?: string;
  size?: number | string;
  style?: CSSProperties;
  // Add other common props like title for accessibility if needed
}
