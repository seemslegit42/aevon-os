"use client";

import AegisSecurityComponent from '@/micro-apps/aegis-security/component';

// This page acts as the route entry point for the self-contained Aegis Security micro-app.
// It cleanly separates the routing concern from the application logic.
export default function AegisSecurityPage() {
  return <AegisSecurityComponent />;
}
